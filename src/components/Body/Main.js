// MainContent.js
import React from 'react';

const MainContent = () => {
  return (
    <main style={mainStyle}>

      {/* Add other main content components or sections */}
    </main>
  );
};

const mainStyle = {
  padding: '20px', // Add some padding for better readability
  backgroundColor: 'transparent', // Set background color to transparent for the particle to be visible
};

const headingStyle = {
  color: 'white', // Text color for the heading
  fontSize: '1em', // Adjust the font size
  marginBottom: '20px', // Add some space below the heading
};

export default MainContent;
